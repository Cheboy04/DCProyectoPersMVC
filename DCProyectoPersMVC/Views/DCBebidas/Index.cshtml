@model IEnumerable<DCProyectoPersMVC.Models.DCBebida>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DC_Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DC_Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DC_Tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DC_Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DC_Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DC_Tipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DC_BebidaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DC_BebidaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DC_BebidaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
